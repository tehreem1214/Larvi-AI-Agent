```toml
[project]
name = "larvi-ai-agent"
version = "0.1.0"
description = "Larvi AI Agent"
requires-python = ">=3.10"

dependencies = [
    "fastapi[standard]",
]

[tool.fastapi]
entrypoint = "app.main:app"

[tool.setuptools.packages.find]
include = ["app*"]
```
